# 2021-12 Semantic Versioning for Interpreted and DSL projects

The idea is to use git tags and the git server itself as source of installationg. Much like Ansible Galaxy with gith+ssh sources using the "version" variable.
